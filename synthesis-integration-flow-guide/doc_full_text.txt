Project Integration Flow History0.1Initial version0.2Update based on HK68882023/9/4Sipaifa & Taotao0.3Update 2025/7/23Panpan Chen1. IP Synthesis Flow本章主要介绍IP级synthesis flow.环境check in/out 目录： $PROJECT_PATH/SYNTHSIS/ip/    Flow以HV1V21为例， HV1V21项目中，top-down synthesis，整个chip可以当作IP看待。需要source cshrc.HV1V21， 这一点和verification 环境保持一致。source cshrc.HV21之前需要修改一下HV1V21_digital.cshrc，注释掉source /eda/source/DRIVER_TEAM/env/Verdi_T-2022.06.cshrc行和source /eda/source/DRIVER_TEAM/env/vcs_T-2022.06.cshrc行，否则run dc和run pt会报错。1.1 gensyn HV1V21    在$PROJECT_PATH/SYNTHSIS/ip/下，执行gensyn HV1V21Tips:初次gen 环境, 如果是在ip目录下, 会自动check in 环境, 这么做初衷是为了svn的内容干净。如果不想check in, 可以gensyn HV1V21 –noadd  (2) 默认文件夹的名字为syn_HK3M31, 如果想自定义名字, 可以gensyn HV1V21 –noadd xxx  (3) 生成的syn_HV1V21里有Readme, 里面有synthesis flow, 可以再次阅读。  (4) 关于环境update, 可以再一次gensyn  HV1V21, 如果template 有更新, 系统会自动copy, 并将原文件做备份, filelist和constraint一般不会更新。1.2配置design.setTOP_NAME 即HV1V21一般gensyn的时候会自动updateUPPER_CASE, PARA_DESIGN, USE_SAIF, POWER_OPTIMIZATION 和其它项目基本相同, 一般情况用default 值就好。SCAN_EN: 综合时一般用default值=0.LINK_LIB 和 TAR_LIB 用于选择综合用的std_cell库, 如果LINK_LIB 需要两种std_cell以上，写法为”nextchip_110_7t nextchip_110_9t”。NET_DEFINE0  一般不需要改, NET_DEFINE1 和clk_rst下的文件相对应。在不同的工具下，需要在一个变量定义，即 NET_DEFINEANA_EN  : analog db enableMEM_EN : memory db enableMEM_SNPS_EN :snps memory db enable, 部分旧项目使用MEM_FARA_EN :fara memory db enable, 部分旧项目使用LVT_EN : lvt cell enable  default is 0。不同项目可能定义RVT，SVT等SVT_EN : svt cell enable  default is 1。不同项目可能不定义SYN_TOOL : “dc” or “genus”CORNER : 综合时一般用default值。Template 中需要根据library配置好，同一个project不同IP使用相同CORNER1.3 填写filelistTips:  (1) HV1V21.f用来填写相关的verilog,   (2) clk_rst中的define 不需要加, 环境自己会加。  (3) memory 和 analog 因为dc 是引用db/lib, 所以filelist也不需要加，lec环境会自己用相关dummy文件。  (4) HV1V21.sv.f用来填写相关system verilog文件。  (5) advanced/HV1V21.vhdl_dc.f advanced/HV1V21.vhdl_lec.f是为vhdl整合做的处理，用法和HV1V21相同, 环境只是source  (6) HV1V21.ddc_dc.f是用于需要引用ddc综合的design。  (7) HV1V21.dummy_lec.f用于lec里需要引用netlist比的文件。  (8) 可以通过run_ckf的方式来补充filelist没有添加的.v。  (9) filelist 里支持嵌套*.f 文件 如下：(10) 强烈推荐：对于底层和中层模块，mem_wrap, bist以及clk_rst, std_ip下相关.v需要自己填写。这么做的初衷是希望owner 自己清楚你的top里有哪些文件。(11) 强烈推荐：能直接调用HDL/file_list/*.f的，可以直接调用，增强仿真和综合的一致性1.4 cd ckfrun_ckfcheck ckf.log &ckf.rptTips: (1) run_ckf需要在run_dc之前。  (1) run_ckf, ckf_rtl.do 一般不需要再修改(2) ckf.rpt中，Floating 必须要修掉,(3) black box 需要double check, 保证都是真实的bbox，通常bbox为analog， memory，digitial macro.(4) Latch 一般只允许kv_gclk.v, io_latch.v 统一引入的latch，或直接调用latch（配合analog design），其它latch需要检查RTL code1.5 cd nlintrun_nlintcheck nlint.log &nlint.rpt  Tips: (1) run_nlint需要在run_dc之前。(2)初始文件在gensyn一般不需要修改， no_check_module.f如果某些module 不需要check, 可以将这些.v 填在里面(3) 鉴于目前的环境会有一些可以不用修改的error,这些items 请先逐个确认然后粘帖到如下文件中，下次nlint rpt就会mask这边error$PROJECT/SYNTHESIS/common/tcl_dir/nlint/nlint_waive.list1.6 cd lec_upf(1) run_nlint不需要在run_dc之前，但建议在run_genus之前(2) HV1V21.upf需要放在SYNTHESIS/common/upf/下，upf的写法参见其它文档。(3) upf的输入文件report_top.txt需要逐行检查。主要问题有   A．library pg pin定义错误。需要改正lib   B．upf文件中analog定义缺失   C．power port电压定义不合理(4) 即便upf修正好，仍可能存在Error和Warning，需要和模拟设计同事确认。1.7 cd dc(1) 经过ckf, nlint, lec_upf, RTL里的问题将会很少(2) run_dc和compile_dc.tcl一般不用修改。Top synthesis需要修改compile_dc.tcl已得到dont_touch.tcl，配合APR需要。(3) 填写constrain $PROJECT_PATH/SYNTHESIS/common/ip_cons/HK3M31.tcl, 内容一般包括：A．kv_clock， kv_v_clock    Clock点必须为macro，standcell的pinB.  kv_resetC.  kv_inoutD.  kv_2clklist_group_path between 2 clock listE.  set_multicycle_pathF.  extra set_false_pathG．set_dont_touch   包括直接引用analog macro，standcell， IO等 ！缺失set_dont_touch将会导致cell被DC优化。(4) 目前环境会有一些common 的cons，例如对MEM scan_mode 等的setting, 这些cons 会统一放到tcl 中, 也可以放在IP contraint中    Run_dc	Check dc.log & *.rpt(6) 在dc.log中，检查A．ErrorB. unresolved (通常问题会在ckf中提前解决)(7) 在*. chk_design中，检查 undrive (通常问题会在ckf中提前解决)(8) 在*.rpt中，检查 A. area. Search”area”,应该符合设计 (需要对设计有所了解)B．timing.  signoff版本需要slack>01.8 cd genus 如果运行DC，genus不需要运行.To be done1.8 cd genus (for upf)但UPF需要genus生成：新建syn_HK3M31_genus_upf(从genus文件夹复制过来)，但需要修改：(1)compile_genus.tcl 注释掉kv_genus_compile “generic”增加read UPF相关的内容同genus， filelist中的HK3M31.f引用的是网表（HK3M31.gv）同genus，syn_HK3M31_genus_upf中引用的是top_cons中的.tcl，它和ip_cons的.tcl一致，但是ip_cons的.tcl中的clk_dbg[0].u_clk_dbg_in_div_0/u_gate_clk_buf/u0/o不识别，必须改为clk_dbg_0_u_clk_dbg_in_div_0/u_gate_clk_buf/u0/o,同理：clk_dbg*0*.u_clk_dbg_in_div_0/u_gate_clk_buf/u0/o 也应该改为clk_dbg_0_u_clk_dbg_in_div_0/u_gate_clk_buf/u0/osetting.upf 一般不需要修改Run_genusCheck genus.log & *.rpt主要是为了生成HK3M31_final.upf1.9 cd lec(1) run_lec和rtl_gate.do一般不用修改。(2)如果做ECO则需要修改rtl_gate.doRun_lecCheck lec.log & *.rpt检查lec.log 中的Error检查abort.rpt 应该是0 个abort point(s), 如果有abort point，经过analyze_abort, 全部解决，也认为是PASS检查non-eq.rpt 应该是0 个non-equivalent point(s)检查unmapped.rpt 应该是0 个unmapped point(s)如果lec不过会自动弹出一个GUI窗口，来分析lec一个简单的看结果的方式：如果PASS，即为过。/(2)如果使用genus synthesis，优先将design.set中的set EC_R2V_EN 1，节约时间，若有 abort无法解决，将design.set中的EC_R2V_EN设为0，分两步进行lec  1.10  signoff 运行目录 $PROJECT_PATH/SYNTHESIS/ip/HK3M31/  Tips:    (1) 会check 你run ckf/nlint/dc/lec时$PROJECT_PATH/HDL 目录的svn number, 已确认所有的整合是否是在一个svn版本下。 (2) check 项在每次run signoff的时候都会打出来, 同时会生成signoff.rpt , 会将error 等check信息整理其中,每一项需要修掉。/(3) signoff pass 后会自动release 到指定目录中$PROJECT_PATH/Release/Netlist/Prescan/HK3M31/svnxxx_20170703_1353 并指向latest, 这么做的初衷是固定大家的release 目录, 免去大家copy 的工作, 但前提是, 你signoff 要pass。(4)如果nlint,ckf,dc,lec不能pass，可以申请用signoff fake。/ (5) silver netlist后不允许signoff fake，紧急情况申请才可以用1.11 cd pt（pre_pt）(1) pt_setup.tcl（在top/sta/pre/pt下）一般不需要修改。修改了set auto_wire_load_selection false （发现pre_pt 的timing 比DC的timing差得多）；如果在ip下的话，pt_setup.tcl对应的是common_pt.tcl(2) 修改common_db_pvt.tcl （一般和综合用一样的Corner：wc_cworst）注意：PVT_MEM 和PVT_ANA可能不一样(HK2N21是一样的)Run_pt	Check pt.log & *.rpt(3) 在pt.log中，检查Error (4) 在*.rpt中，检查 timing.  slack>02.2 cd sta/post/pt(1) 修改corner_set.tcl,主要修改CORNER （wc:LVHT(低压高温)；wcl:LVLT; lt(也叫bc):HVLT; ml:HVHT; tt:中温中压 ）(2)修改pt_setup.tcl 中的read_verilog内容（主要是APR吐出的.v.gv）；set_operating_conditions(和DataBase/Analog/DB的**.lib中的operating_conditions一致)。(3) 修改pt/design.set (post pt 自己独有的)A．路径B.  LINK_LIBC.  TAR_LIBD.  NET_DEFINE E.  POSTPT_EN 必须为1F.  PT_HOLD 只是针对22nm工艺(4) 修改common_db_pvt.tcl 注意：PVT_MEM 和PVT_ANA可能不一样Run_pt	Check pt.log & *.rpt(5) 在pt.log中，检查Error (6) 在*.rpt中，检查 timing.  slack>02.整体流程其中ckf nlint lec_upf  dc(genus) 可以并行做 ，pre_pt  lec可以并行做。但是为了signoff 成功（version check）,dc（genus） 要在ckf和nlint 之后运行。3 注意事项：1. Lec_upf 主要是检查自己写的upf是否正确，genus_upf是利用run_genus生成xxx_final.upf，其中genus_upf的file_list引用的是网表（xxx.gv）或rtl，如果引用的是xxx.gv，可以在注释掉 kv_genus_compile “generic” ,打开upf。如果引用的是rtl，则必须全部打开。2. 综合时代码中的output，inout 不能接常量，运行genus_upf（ file_list引用的是网表（xxx.gv））会报错3. 运行综合时如果整个module只有input，会被优化掉，可以设置set_dont_touch4. set_ideal_network 一般用在clk、reset，让工具不插buffer  dont_touch 一般用在analog cell ,不让工具优化 dont_touch_network 同dont_touch，但范围更广，直到DFF false_path 一般用在两个异步时钟之间。4.memory 和mem_wrap的生成：拷贝于https://172.16.185.191/svn/BE/IP_team/flow/mem_gen_flow/HK2N21 mem gen flow.docx以HK3M31 （110nm）的工艺为例子：利用工具生成memory：在/project/HK2N21/designer/DD/zkji/HK2N21/DFT/memory_compiler/ 下执行run命令，弹出窗口填写：A. word,，bit ，byte(只写1)；B. ring layer: 2233（生成的memory是横向的）；3322（生成的memory是竖向的）C. bus format 必须选YES（生成的bus信号是给你包好的）D. 下一步feature 选择GenericF. 然后按一步步操作即可。（2）整理lib ,生成DB     在/project/HK2N21/designer/DD/zkji/HK2N21/DFT/project_mem_db/copy_tcl下修改 sram.list  corner.list修改 gen_tcl.pl执行 gen_tcl.pl 生成了 cp_lib和gen_db_tcl新建文件夹 cfg cir db ds dummy gds lef lib lvlib sim pglib执行 cp_lib执行bsub –Ip run_lc (生成db，单独的db，可以不做这一步，直接执行G) Merge lib and db: 在/project/HK2N21/designer/DD/zkji/HK2N21/DFT/project_mem_db/mem_db/lib/下执行 do_merge_db（生成merged lib和db,放在**_merged文件夹下）；  do_merge_back（将**_merged文件夹下生成的merged lib和db复制到对应的**文件夹下 ）（3）将mem_db 整个文件夹复制到database下（注意不要复制**_merged文件夹，如ff1p65v0c_merged; 只复制对应的**文件夹,如ff1p65v0c）/project/HK2N21/DataBase/Digital/memory_library/nexchip/ （4）生成mem_wrap     A. 修改/project/HK2N21/designer/DD/zkji/HK2N21/DFT/mbist/mem_info_110/mem_wrap/nexchip/wrapper/ 下的memory_wrapper.config。注意：        只需修改第三列名称wrapper_name固定*p看后面snps_22_后面是*p就写几；msize就是depth；mwide是width，（注意看第七列byte_num是1的话就只写相应的width;若非1就需要写1x相应的width）；再看看第9列mem_num是几就写x几(如：wrappe_name mem_wrap_1p_208_640_x2)；最后在文档最后一列之后添加pg_en、repair两列这两列默认值为0.     B. Source mem_wrap下的run.tcl (注意命令是：source run.tcl) 生成wrapper.v 和fault_info_mux.v hv_mem_bist_info.v     C. 将生成的.v复制到HDL/mem_wrap/ 文件下     5.功耗仿真：路径在$(PROJECT_PATH)/SYNTHESIS/top/power/0.px_template/下，其中有两个cornet分别是px_125ml和px_85c。我们以px_85c为例：需要注意修改的地方：1.design.set中的TOP_NAME，改为HK3M31_digital_top2.px_cfg.tcl中各个路径需要修改：具体如下:(1) verilog_file的路径为PR同事release的网表；(2) sdc_file是数字同事Release目录下的HK3M31_digital_top_postcts.sdc文件，需要注意的是需要修改一下该文件中的sdc_version，需改为2.0才能够仿真； (3) spec_dir 为PR同事的release目录，/project/HK3M31/designer/AD/llwang/release/HK3M31_digital_top/timing/latest;    (4) 生成vcd之前，需要在post pt相应corner生成sdf文件，然后在post sim case里面读取sdf；(5) ip0_inst改为HK3M31_digital_top；ip0_strip改为chip_tb_top/u_HK3M31_top_beh/u_HK3M31_digital_top；ip0_vcd改为vcd波形所在的路径(vsd的文件类型为：verilog.dump)；3. 关于怎么dump vcd波形：只需要将原来dump fsdb的函数换成$dumpvars即可（注：也可以正常dump fsdb，然后在波形里找到信号频繁翻转的时间区间，截取得到vcd文件，例如：想截取chip_tb_top_000.fsdb里从500ps到700ps的vcd，用该语法 fsdb2vcd chip_tb_top_000.fsdb –bt 500ps –et 700ps –o Verilog.dump）；4. 修改完成后执行run_ptpx;跑完仿真后的report在rep_dir目录下，HK3M31_digital_top_toggle_rate_detail.rpt可以查看toggle rate；HK3M31_digital_top_power.rpt查看功耗结果；上一层目录会生成fsdb文件，可以用verdi打开，查看整个仿真时间的各个模块的功耗波形；